import {JwtDto} from "../../../services/auth/AuthDto";


interface NewBooksProps {
    jwtPayload: JwtDto
}

const NewBooks = (props: NewBooksProps) => {
    return (<div></div>);
}

export default NewBooks;